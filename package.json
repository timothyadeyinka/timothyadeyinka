{
  "__note": "only use the code below to push new changes to remote-side of repo",
  "__command": "git config alias.deploy 'subtree split --prefix dist -b deploy'",
  "name": "portfolio-website",
  "version": "1.0.0",
  "description": "Portfolio Website",
  "main": "index.js",
  "scripts": {
    "compile:sass": "sass scss/main.scss ../css/style.css -w"
  },
  "author": "Accurate A",
  "license": "ISC",
  "dependencies": {
    "sass": "^1.83.0"
  }
}
