{
  "__note": "only use the codes below accordingly to push new changes after staging and commits has been done to the remote-side of repo",
  "__command1": "git subtree split --prefix dist -b deploy",
  "__command2": "git push origin deploy:main --force",
  "name": "portfolio-website",
  "version": "1.0.0",
  "description": "Portfolio Website",
  "main": "index.js",
  "scripts": {
    "compile:sass": "sass scss/main.scss ../css/style.css -w"
  },
  "author": "Timothy Adeyinka",
  "license": "ISC",
  "dependencies": {
    "sass": "^1.83.0"
  }
}
